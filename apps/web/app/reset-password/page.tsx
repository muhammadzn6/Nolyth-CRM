import type { Metadata } from "next";
import { PasswordResetForm } from "../../components/auth/password-reset-form";
export const metadata: Metadata = { title: "Reset password" };
export default async function ResetPasswordPage({ searchParams }: { searchParams?: Promise<{ token?: string }> }) { const token = (await searchParams)?.token; return <main className="grid min-h-screen place-items-center bg-background p-6"><section className="w-full max-w-md rounded-2xl border border-border bg-surface p-7 shadow-sm"><p className="text-xs font-bold uppercase tracking-[0.16em] text-primary">Orbit account</p><h1 className="mt-3 text-2xl font-bold text-foreground">{token ? "Choose a new password" : "Reset your password"}</h1><p className="mt-2 text-sm text-muted-foreground">{token ? "Use at least 12 characters." : "Enter your work email and we’ll provide reset instructions if the account exists."}</p><div className="mt-6"><PasswordResetForm token={token} /></div></section></main>; }
